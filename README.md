# OCS Notes Frontend

Simple frontend for OCS Tracker notes.  
- Uses your Render backend for GPT-powered Setup Summary correction.
- Copy full notes to clipboard.

## Usage
1. Update `index.html` → replace `YOUR_BACKEND_URL` with your backend URL.  
2. Open `index.html` in browser, or deploy as a static site on Render.  

## Features
- Dropdowns for integration type & status
- YES/NO buttons for Server Access & Calendar Sync
- GPT grammar & bullet point correction for Setup Summary
- Copy full note to clipboard
